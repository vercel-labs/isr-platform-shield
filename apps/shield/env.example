# Core app hostname for Shield rewrites (see vercel.ts)
# Use the host from coreUrl in config/validation.json
CORE_HOST=core.your-domain.com

# Only needed for `pnpm deploy:shield`
VERCEL_TOKEN=your_vercel_token_here
