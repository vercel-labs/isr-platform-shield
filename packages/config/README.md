# @platform/config

Loads `config/validation.json` from the repo root. See [config/README.md](../../config/README.md) for the schema.

```typescript
import { config, urls, getConfig, tryGetConfig } from "@platform/config";
```
